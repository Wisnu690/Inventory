import 'package:flutter/material.dart';

class UserItems extends StatelessWidget {
  const UserItems ({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Halaman Items User"),
    );
  }
}